<?php

?>
<script type="text/javascript">
    <!--
    function xoopsCheckAllElementsButton(button, name) {
    var box, e = 0, grp = button.form[name];
    while (box = grp[e++])
    box.checked = (button.value == 'Check All');

    return (button.value == 'Check All') ? 'Uncheck All' : 'Check All';
    }
    //-->
</ script>;
